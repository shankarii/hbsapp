<div class="container">
    <div class="row">
        {{#each imageArray}}
        <div class="col-md-6 fix-height">
            <div class="thumbnail">
                <img src="/images/{{../city}}/{{../city}}-{{this}}.jpg">
            </div>
        </div>
        {{/each}}
    </div>
</div>